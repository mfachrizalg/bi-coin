# Frontend language and interface design

The frontend uses concise English authored copy and a restrained central-bank utility interface. Institutional and retail screens share the same visual system but use different density, navigation uses a compact top bar, motion is omitted, and initial remote reads use static skeletons. Backend-provided error text remains unchanged, while authentication, API contracts, role boundaries, routes, and guided-demo behavior remain unchanged.
